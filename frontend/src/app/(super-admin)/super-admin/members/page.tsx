import MembersView from '@/components/views/MembersView';

export const metadata = { title: 'All Members | ISMS Super Admin' };

export default function SuperAdminMembersPage() {
  return <MembersView />;
}
