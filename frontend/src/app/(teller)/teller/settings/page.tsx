import SettingsView from '@/components/views/SettingsView';

export const metadata = { title: 'Settings | Teller Portal' };

export default function TellerSettingsPage() {
  return <SettingsView />;
}
