import PortalPlaceholder from '@/components/layout/PortalPlaceholder';

export default function TenantAdminDashboardPage() {
  return (
    <PortalPlaceholder
      eyebrow="Biruk · Task 21"
      title="Tenant Admin dashboard"
      description="Member counts, account summary, and pending approvals will land here. This page is an empty shell from Task 7."
    />
  );
}
